use chroot = yes
log file = /storage/logs/rsyncd.log
hosts allow = 127.0.0.1

[live]
    path = /storage/rsyncroot/live
    uid = 1000
    gid = 1000
    read only = no
    list = yes

[cron]
    path = /storage/rsyncroot/cron
    uid = 1000
    gid = 1000
    read only = no
    list = yes
